interface:
  display_name: "Evaluate Suggestions"
  short_description: "Evaluate user feedback before accepting or rejecting it."
  default_prompt: "Use $evaluate-suggestions to assess this suggestion before acting on it."

policy:
  allow_implicit_invocation: true
