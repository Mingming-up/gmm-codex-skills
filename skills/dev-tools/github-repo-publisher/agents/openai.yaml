interface:
  display_name: "GitHub Repo Publisher"
  short_description: "Create GitHub repos and push local projects."
  default_prompt: "Use $github-repo-publisher to create a GitHub repository for this local project and push it."
